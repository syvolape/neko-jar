/** Route entry for the home screen. The actual UI lives in `components/screens/home-screen`. */

import HomeScreen from "@/components/screens/home-screen";

export default function HomePage() {
  return <HomeScreen />;
}
