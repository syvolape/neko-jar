/** Route entry for create-goal step 2 (`/create-goal/amount`). The form UI is kept in the shared screen component. */

import CreateGoalAmountScreen from "@/components/screens/create-goal-amount-screen";

export default function CreateGoalAmountPage() {
  return <CreateGoalAmountScreen />;
}
